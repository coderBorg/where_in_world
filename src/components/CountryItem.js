import React from 'react'

export const CountryItem = ({name}) => {
    return (
        <div>
            <p>Country item</p>
            <p>Name: {name}</p>
        </div>
    );
}

export default CountryItem;